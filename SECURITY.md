# 安全与隐私说明

ZenDiary 的数据默认保存在本机选定的数据目录中的 Hive 文件里。应用没有内置账号体系或云端同步；桌面与 Android 设备各自保存本地数据，跨设备需要用户手动导出和导入 JSON 备份。数据目录是否加密取决于操作系统、磁盘和用户账户设置，便携版数据文件也不自动加密。

未启用或未配置 AI 时，收件箱使用本地规则；启用且配置完成后，提交给 AI 整理的原文和当前时间会发送到用户配置的兼容 `/chat/completions` 服务，失败时再回退本地规则。远程服务必须使用 HTTPS；仅明确的 `localhost`、`127.0.0.1` 或 `::1` 允许使用 HTTP。自定义服务的日志、留存和访问控制由服务提供方负责。

API Key 当前保存在本机 Hive 设置中，应用运行时会以 `Authorization` 请求头发送；本版本没有引入跨平台安全存储依赖，因此本地有权限读取应用数据目录的进程可能读取它。请不要把 Key 放入 Base URL 的账号、查询参数或片段中。导出备份会移除 API Key，并清理 Base URL 中的账号信息和查询字符串。导入备份时，如果本机已有 AI 配置，会完整保留本机的服务地址、模型、启用状态和 Key；没有本机配置时会丢弃备份 Key 并保持 AI 关闭，避免备份改变服务端点后带出本机凭据。JSON 备份仍可能包含日记、待办、笔记和提醒内容，请像保存原始资料一样保护备份文件。

旧版内置的无认证本机 MCP HTTP 服务已经禁用，不再提供读取笔记或待办的监听入口。应用中的提醒和本地数据操作仍走内部 provider 与本地存储路径。

如发现安全问题，请在公开仓库提交不含密钥、个人内容或完整备份的 issue，说明受影响版本、复现步骤和影响范围。需要共享日志时请先删除 API Key、URL 凭据、原文、文件路径和个人标识；不要把真实凭据粘贴到 issue、提交记录或讨论中。
